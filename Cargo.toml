[package]
name = "lau-biome"
version = "0.1.0"
edition = "2021"
description = "Biome system for the LAU game — distinct ecological zones with unique properties, resources, and mathematical characteristics."

[dependencies]
serde = { version = "1", features = ["derive"] }
rand = "0.8"
rand_xoshiro = "0.6"

[dev-dependencies]
serde_json = "1.0.150"
